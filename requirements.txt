# Flask: Web framework to build the backend
Flask==2.3.2  

# Pandas: For data manipulation and preprocessing
pandas==2.0.3  

# Scikit-learn: For machine learning model training and prediction
scikit-learn==1.3.0  

# NLTK: For natural language processing tasks like tokenization and stopword removal
nltk==3.8.1  

# Joblib: To save and load machine learning models and vectorizers
joblib==1.3.0  

# Werkzeug: For handling web requests (comes with Flask, added for compatibility)
Werkzeug==2.3.7  

# Jinja2: Template engine used by Flask for rendering HTML files
Jinja2==3.1.2  

# MarkupSafe: Required for Flask and Jinja2 compatibility
MarkupSafe==2.1.3  

# ItsDangerous: Required for Flask security mechanisms
itsdangerous==2.1.2  

# Click: Used internally by Flask for command-line interface support
Click==8.1.3  

# NumPy: Provides support for numerical operations (used internally by pandas and scikit-learn)
numpy==1.25.2  

# Requests: For making HTTP requests (optional but useful for API integrations)
requests==2.31.0  
